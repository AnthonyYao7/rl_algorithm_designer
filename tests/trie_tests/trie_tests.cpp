//
// Created by anthony on 7/25/23.
//


#include <string>

#include <gtest/gtest.h>

#include "rl_environment/Trie.h"


TEST(TrieInsertionTest, TrieTests)
{
//    Trie<std::string> t;
//
//    t.insert_sequence({"postfix_expression", "[", "expression", "]"});
//    t.insert_sequence({"postfix_expression", "(", ")"});
//    t.insert_sequence({"postfix_expression", "(", "argument_expression_list", ")"});
//
//    auto root = t.get_root();
//
//    EXPECT_EQ(root->children[0]->value, "postfix_expression");
//
//    EXPECT_EQ(root->children[0]->children[0]->value, "[");
//    EXPECT_EQ(root->children[0]->children[0]->children[0]->value, "expression");
//    EXPECT_EQ(root->children[0]->children[0]->children[0]->children[0]->value, "]");
//
//    EXPECT_EQ(root->children[0]->children[1]->value, "(");
//    EXPECT_EQ(root->children[0]->children[1]->children[0]->value, ")");
//    EXPECT_EQ(root->children[0]->children[1]->children[1]->value, "argument_expression_list");
//    EXPECT_EQ(root->children[0]->children[1]->children[1]->children[0]->value, ")");
}